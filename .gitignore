# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# production
build/
dist/

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / scratch
tmpclaude-*
nul
.claude/

# OS
Thumbs.db

# vendored 3rd-party binaries — fetched by npm prebuild
vendor/cloudflared/cloudflared.exe

# Panasonic Lumix Remote Control SDK — licensed, machine-local, NOT redistributable in-repo
LumixRemoteControlLibraryBeta1.00/
# Native camera bridge build outputs (exe + the SDK runtime DLL copied beside it)
vendor/camera-bridge/promptly-camera-bridge.exe
vendor/camera-bridge/*.dll
vendor/camera-bridge/*.obj
# Native Raw Input helper build output
vendor/input-bridge/promptly-input-bridge.exe
vendor/input-bridge/*.obj
